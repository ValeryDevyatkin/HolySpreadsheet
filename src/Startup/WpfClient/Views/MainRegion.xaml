<UserControl x:Class = "WpfClient.Views.MainRegion"
             xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x = "http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc = "http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d = "http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels = "clr-namespace:ViewModels.ViewModels;assembly=ViewModels"
             xmlns:theme = "clr-namespace:WpfClient.Theme"
             xmlns:regions = "clr-namespace:WpfClient.Views.Regions"
             mc:Ignorable = "d">

    <d:Control.DataContext>
        <viewModels:MainViewModel />
    </d:Control.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width = "*" />
            <ColumnDefinition Width = "{x:Static theme:UiConstants.OuterSpacingGridLength}" />
            <ColumnDefinition Width = "*" />
            <ColumnDefinition Width = "{x:Static theme:UiConstants.OuterSpacingGridLength}" />
            <ColumnDefinition Width = "*" />
        </Grid.ColumnDefinitions>

        <regions:InputRegion Grid.Column = "0" />
        <regions:GridRegion Grid.Column = "2" />
        <regions:OutputRegion Grid.Column = "4" />
    </Grid>

</UserControl>